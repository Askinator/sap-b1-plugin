# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local Claude / plugin state
.claude/
/.vs
# Ignore local .github state but keep CI workflows tracked
/.github/*
!/.github/workflows/

# Node — only used to pin the claude CLI for `plugin validate`, not part of the plugin
node_modules/
package.json
package-lock.json
